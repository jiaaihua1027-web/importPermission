{
  "name": "permission-import-tool",
  "version": "1.0.0",
  "description": "权限导入工具",
  "private": true,
  "scripts": {
    "install": "npm install --legacy-peer-deps",
    "build": "npm install --legacy-peer-deps && vite build",
    "deploy": "npm install --legacy-peer-deps && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "antd": "^5.12.0",
    "@ant-design/icons": "^5.2.6"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
